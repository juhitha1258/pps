#include<stdio.h>
void main()
{
int a;
char b;
float c;
double d;
printf("the size of int and char are %lu %lu \n",sizeof(a),sizeof(b));
printf("the size of float and double %lu %lu \n",sizeof(c),sizeof(d));
}
